{
  "name": "vuvas",
  "version": "0.1.0",
  "description": "Canvas 上的 Vue —— 让开发者用类 Vue 的方式开发渲染在 Canvas 上的 UI",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.2.0"
  }
}
